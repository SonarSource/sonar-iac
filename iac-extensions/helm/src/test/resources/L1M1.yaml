apiVersion: v1
kind: Pod
metadata:
  name: example
spec:
  containers:
    - name: web
      image: nginx
      ports:
        - name: web
          containerPort: {{ .Values.container.port }}  # simple Helm template
          protocol: TCP
      securityContext:
        allowPrivilegeEscalation: true # Sensitive S6428
