# Noncompliant@+1 {{Make sure disabling the "secure" flag of this cookie is safe here.}}
server.servlet.session.cookie.secure=false
#^[sc=0;ec=43]

#---

# Noncompliant@+1 {{Make sure disabling the "secure" flag of this cookie is safe here.}}
server.servlet.session.cookie.secure=fAlSe
#^[sc=0;ec=43]

#---
# Compliant, as values in properties file don't remove quotes
server.servlet.session.cookie.secure="false"

#---
server.servlet.session.cookie.secure=true
#^[sc=0;ec=43]


#---
server.servlet.session.cookie.secure=5

#---
# Compliant as key does not match
server.servlet.session.cookie.unsecure=false

#---
server.servlet.session.cookie.secure=
