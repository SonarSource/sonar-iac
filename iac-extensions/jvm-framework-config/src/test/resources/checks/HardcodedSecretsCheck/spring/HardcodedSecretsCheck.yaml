spring:
  mail:
    password: password # Noncompliant {{Revoke and change this password, as it is compromised.}}
  #           ^^^^^^^^
  sendgrid:
    api-key: password # Noncompliant
  cassandra:
    password: password # Noncompliant
  couchbase:
    password: password # Noncompliant
    authentication:
      jks:
        password: password # Noncompliant
  data:
    mongodb:
      password: password # Noncompliant
    redis:
      password: password # Noncompliant
      sentinel:
        password: password # Noncompliant
  datasource:
    hikari:
      password: password # Noncompliant
    password: password # Noncompliant
    tomcat:
      password: password # Noncompliant
    dbcp2:
      password: password # Noncompliant
  elasticsearch:
    password: password # Noncompliant
    api-key: api-key # Noncompliant
  h2:
    console:
      settings:
        web-admin-password: password # Noncompliant
  ldap:
    embedded:
      credential:
        password: password # Noncompliant
    password: password # Noncompliant
  mongodb:
    password: password # Noncompliant
  neo4j:
    authentication:
      password: password # Noncompliant
  r2dbc:
    password: password # Noncompliant
  flyway:
    password: password # Noncompliant
  liquibase:
    password: password # Noncompliant
    license-key: license-key # Noncompliant
  sql:
    init:
      password: password # Noncompliant
  activemq:
    password: password # Noncompliant
  artemis:
    password: password # Noncompliant
    embedded:
      cluster-password: password # Noncompliant
  kafka:
    admin:
      ssl:
        key-password: password # Noncompliant
        key-store-password: password # Noncompliant
        trust-store-password: password # Noncompliant
    consumer:
      ssl:
        key-password: password # Noncompliant
        key-store-key: password # Noncompliant
        key-store-password: password # Noncompliant
        trust-store-password: password # Noncompliant
    producer:
      ssl:
        key-password: password # Noncompliant
        key-store-key: password # Noncompliant
        key-store-password: password # Noncompliant
        trust-store-password: password # Noncompliant
    ssl:
      key-password: password # Noncompliant
      key-store-password: password # Noncompliant
      trust-store-password: password # Noncompliant
    streams:
      ssl:
        key-password: password # Noncompliant
        key-store-key: password # Noncompliant
        key-store-password: password # Noncompliant
        trust-store-password: password # Noncompliant
  rabbitmq:
    password: password # Noncompliant
    ssl:
      key-store-password: password # Noncompliant
      trust-store-password: password # Noncompliant
    stream:
      password: password # Noncompliant
  security:
    oauth2:
      resourceserver:
        opaquetoken:
          client-secret: password # Noncompliant
    user:
      password: password # Noncompliant
  rsocket:
    server:
      ssl:
        key-password: password # Noncompliant
        key-store-password: password # Noncompliant
        trust-store-password: password # Noncompliant
  devtools:
    remote:
      secret: password # Noncompliant
  ai:
    anthropic:
      api-key: api-key # Noncompliant
    azure:
      openai:
        api-key: api-key # Noncompliant
    bedrock:
      aws:
        secret-key: api-key # Noncompliant
    deepseek:
      api-key: api-key # Noncompliant
    huggingface:
      chat:
        api-key: api-key # Noncompliant
    minimax:
      api-key: api-key # Noncompliant
    mistralai:
      api-key: api-key # Noncompliant
    openai:
      api-key: api-key # Noncompliant
      api:
        key: api-key # Noncompliant
    oci:
      genai:
        passPhrase: passphrase # Noncompliant
    qianfan:
      api-key: api-key # Noncompliant
      secret-key: api-key # Noncompliant
    vectorstore:
      chroma:
        client:
          key-token: key-token # Noncompliant
      opensearch:
        password: password # Noncompliant
      pinecone:
        apiKey: api-key # Noncompliant
    zhipu:
      api-key: api-key # Noncompliant
server:
  ssl:
    key-password: password # Noncompliant
    key-store-password: password # Noncompliant
    trust-store-password: password # Noncompliant
management:
  appoptics:
    metrics:
      export:
        api-token: password # Noncompliant
  datadog:
    metrics:
      export:
        api-key: password # Noncompliant
        application-key: password # Noncompliant
  dynatrace:
    metrics:
      export:
        api-token: password # Noncompliant
  elastic:
    metrics:
      export:
        api-key-credentials: password # Noncompliant
        password: password # Noncompliant
  humio:
    metrics:
      export:
        api-token: password # Noncompliant
  influx:
    metrics:
      export:
        password: password # Noncompliant
        token: password # Noncompliant
  kairos:
    metrics:
      export:
        password: password # Noncompliant
  newrelic:
    metrics:
      export:
        api-key: password # Noncompliant
  prometheus:
    metrics:
      export:
        pushgateway:
          password: password # Noncompliant
          token: token # Noncompliant
  server:
    ssl:
      key-password: password # Noncompliant
      key-store-password: password # Noncompliant
      trust-store-password: password # Noncompliant
  signalfx:
    metrics:
      export:
        access-token: password # Noncompliant
  wavefront:
    api-token: password # Noncompliant

---

spring:
  mail:
    password: ${PASSWORD}
  sendgrid:
    api-key: ${PASSWORD}
  cassandra:
    password: ${PASSWORD}
  couchbase:
    password: ${PASSWORD}
    authentication:
      jks:
        password: ${PASSWORD}
  data:
    mongodb:
      password: ${PASSWORD}
    redis:
      password: ${PASSWORD}
      sentinel:
        password: ${PASSWORD}
  datasource:
    hikari:
      password: ${PASSWORD}
    password: ${PASSWORD}
    tomcat:
      password: ${PASSWORD}
    dbcp2:
      password: ${PASSWORD}
  elasticsearch:
    password: ${PASSWORD}
    api-key: ${APIKEY}
  h2:
    console:
    settings:
      web-admin-password: ${PASSWORD}
  ldap:
    embedded:
      credential:
        password: ${PASSWORD}
    password: ${PASSWORD}
  mongodb:
    password: ${PASSWORD}
  neo4j:
    authentication:
      password: ${PASSWORD}
  r2dbc:
    password: ${PASSWORD}
  flyway:
    password: ${PASSWORD}
  liquibase:
    password: ${PASSWORD}
    license-key: ${LICENSE_KEY}
  sql:
    init:
      password: ${PASSWORD}
  activemq:
    password: ${PASSWORD}
  artemis:
    password: ${PASSWORD}
    embedded:
      cluster-password: ${PASSWORD}
  kafka:
    admin:
      ssl:
        key-password: ${PASSWORD}
        key-store-password: ${PASSWORD}
        trust-store-password: ${PASSWORD}
    consumer:
      ssl:
        key-password: ${PASSWORD}
        key-store-key: ${PASSWORD}
        key-store-password: ${PASSWORD}
        trust-store-password: ${PASSWORD}
    producer:
      ssl:
        key-password: ${PASSWORD}
        key-store-key: ${PASSWORD}
        key-store-password: ${PASSWORD}
        trust-store-password: ${PASSWORD}
    ssl:
      key-password: ${PASSWORD}
      key-store-password: ${PASSWORD}
      trust-store-password: ${PASSWORD}
    streams:
      ssl:
        key-password: ${PASSWORD}
        key-store-key: ${PASSWORD}
        key-store-password: ${PASSWORD}
        trust-store-password: ${PASSWORD}
  rabbitmq:
    password: ${PASSWORD}
    ssl:
      key-store-password: ${PASSWORD}
      trust-store-password: ${PASSWORD}
    stream:
      password: ${PASSWORD}
  security:
    oauth2:
      resourceserver:
        opaquetoken:
          client-secret: ${PASSWORD}
    user:
      password: ${PASSWORD}
  rsocket:
    server:
      ssl:
        key-password: ${PASSWORD}
        key-store-password: ${PASSWORD}
        trust-store-password: ${PASSWORD}
  devtools:
    remote:
      secret: ${PASSWORD}
  ai:
    anthropic:
      api-key: ${API_KEY}
    azure:
      openai:
        api-key: ${API_KEY}
    bedrock:
      aws:
        access-key: YOUR_ACCESS_KEY
        secret-key: ${API_KEY}
    deepseek:
      api-key: ${API_KEY}
    huggingface:
      chat:
        api-key: ${API_KEY}
    minimax:
      api-key: ${API_KEY}t
    mistralai:
      api-key: ${API_KEY}
    openai:
      api-key: ${API_KEY}
      api:
        key: ${API_KEY}
    oci:
      genai:
        passPhrase: ${PASSPHRASE}
    qianfan:
      api-key: ${API_KEY}
      secret-key: ${API_KEY}
    vectorstore:
      chroma:
        client:
          key-token: ${KEY_TOKEN}
      opensearch:
        password: ${PASSWORD}
      pinecone:
        apiKey: ${API_KEY}
    zhipu:
      api-key: ${API_KEY}
server:
  ssl:
    key-password: ${PASSWORD}
    key-store-password: ${PASSWORD}
    trust-store-password: ${PASSWORD}
management:
  appoptics:
    metrics:
      export:
        api-token: ${PASSWORD}
  datadog:
    metrics:
      export:
        api-key: ${PASSWORD}
        application-key: ${PASSWORD}
  dynatrace:
    metrics:
      export:
        api-token: ${PASSWORD}
  elastic:
    metrics:
      export:
        api-key-credentials: ${PASSWORD}
        password: ${PASSWORD}
  humio:
    metrics:
      export:
        api-token: ${PASSWORD}
  influx:
    metrics:
      export:
        password: ${PASSWORD}
        token: ${PASSWORD}
  kairos:
    metrics:
      export:
        password: ${PASSWORD}
  newrelic:
    metrics:
      export:
        api-key: ${PASSWORD}
  prometheus:
    metrics:
      export:
        pushgateway:
          password: ${PASSWORD}
          token: ${TOKEN}
  server:
    ssl:
      key-password: ${PASSWORD}
      key-store-password: ${PASSWORD}
      trust-store-password: ${PASSWORD}
  signalfx:
    metrics:
      export:
        access-token: ${PASSWORD}
  wavefront:
    api-token: ${PASSWORD}

---
spring:
  datasource:
    password: ${PASSWORD}-suffix # Compliant

---
spring:
  datasource:
    password: prefix-${PASSWORD} # Compliant

---
spring:
  datasource:
    password: # Compliant

---
spring:
  datasource:
    password: '' # Compliant

---
spring:
  datasource:
    password: "" # Compliant

---
spring:
  datasource:
    password: ${PASSWORD:defaultValue} # Compliant
