# Noncompliant@+1 {{Revoke and change this password, as it is compromised.}}
spring.datasource.password=dbpassword
#                          ^^^^^^^^^^
# Noncompliant@+1
spring.mail.password=password
# Noncompliant@+1
spring.sendgrid.api-key=password
# Noncompliant@+1
spring.cassandra.password=password
# Noncompliant@+1
spring.couchbase.password=password
# Noncompliant@+1
spring.data.mongodb.password=password
# Noncompliant@+1
spring.data.redis.password=password
# Noncompliant@+1
spring.data.redis.sentinel.password=password
# Noncompliant@+1
spring.datasource.hikari.password=password
# Noncompliant@+1
spring.datasource.password=password
# Noncompliant@+1
spring.datasource.tomcat.password=password
# Noncompliant@+1
spring.elasticsearch.password=password
# Noncompliant@+1
spring.h2.console.settings.web-admin-password=password
# Noncompliant@+1
spring.ldap.embedded.credential.password=password
# Noncompliant@+1
spring.ldap.password=password
# Noncompliant@+1
spring.neo4j.authentication.password=password
# Noncompliant@+1
spring.r2dbc.password=password
# Noncompliant@+1
spring.flyway.password=password
# Noncompliant@+1
spring.liquibase.password=password
# Noncompliant@+1
spring.sql.init.password=password
# Noncompliant@+1
spring.activemq.password=password
# Noncompliant@+1
spring.artemis.password=password
# Noncompliant@+1
spring.kafka.admin.ssl.key-password=password
# Noncompliant@+1
spring.kafka.admin.ssl.key-store-password=password
# Noncompliant@+1
spring.kafka.admin.ssl.trust-store-password=password
# Noncompliant@+1
spring.kafka.consumer.ssl.key-password=password
# Noncompliant@+1
spring.kafka.consumer.ssl.key-store-key=password
# Noncompliant@+1
spring.kafka.consumer.ssl.key-store-password=password
# Noncompliant@+1
spring.kafka.consumer.ssl.trust-store-password=password
# Noncompliant@+1
spring.kafka.producer.ssl.key-password=password
# Noncompliant@+1
spring.kafka.producer.ssl.key-store-key=password
# Noncompliant@+1
spring.kafka.producer.ssl.key-store-password=password
# Noncompliant@+1
spring.kafka.producer.ssl.trust-store-password=password
# Noncompliant@+1
spring.kafka.ssl.key-password=password
# Noncompliant@+1
spring.kafka.ssl.key-store-password=password
# Noncompliant@+1
spring.kafka.ssl.trust-store-password=password
# Noncompliant@+1
spring.kafka.streams.ssl.key-password=password
# Noncompliant@+1
spring.kafka.streams.ssl.key-store-key=password
# Noncompliant@+1
spring.kafka.streams.ssl.key-store-password=password
# Noncompliant@+1
spring.kafka.streams.ssl.trust-store-password=password
# Noncompliant@+1
spring.rabbitmq.password=password
# Noncompliant@+1
spring.rabbitmq.ssl.key-store-password=password
# Noncompliant@+1
spring.rabbitmq.ssl.trust-store-password=password
# Noncompliant@+1
spring.rabbitmq.stream.password=password
# Noncompliant@+1
server.ssl.key-password=password
# Noncompliant@+1
server.ssl.key-store-password=password
# Noncompliant@+1
server.ssl.trust-store-password=password
# Noncompliant@+1
spring.security.oauth2.resourceserver.opaquetoken.client-secret=password
# Noncompliant@+1
spring.security.user.password=password
# Noncompliant@+1
spring.rsocket.server.ssl.key-password=password
# Noncompliant@+1
spring.rsocket.server.ssl.key-store-password=password
# Noncompliant@+1
spring.rsocket.server.ssl.trust-store-password=password
# Noncompliant@+1
management.appoptics.metrics.export.api-token=password
# Noncompliant@+1
management.datadog.metrics.export.api-key=password
# Noncompliant@+1
management.datadog.metrics.export.application-key=password
# Noncompliant@+1
management.dynatrace.metrics.export.api-token=password
# Noncompliant@+1
management.elastic.metrics.export.api-key-credentials=password
# Noncompliant@+1
management.elastic.metrics.export.password=password
# Noncompliant@+1
management.humio.metrics.export.api-token=password
# Noncompliant@+1
management.influx.metrics.export.password=password
# Noncompliant@+1
management.influx.metrics.export.token=password
# Noncompliant@+1
management.kairos.metrics.export.password=password
# Noncompliant@+1
management.newrelic.metrics.export.api-key=password
# Noncompliant@+1
management.prometheus.metrics.export.pushgateway.password=password
# Noncompliant@+1
management.server.ssl.key-password=password
# Noncompliant@+1
management.server.ssl.key-store-password=password
# Noncompliant@+1
management.server.ssl.trust-store-password=password
# Noncompliant@+1
management.signalfx.metrics.export.access-token=password
# Noncompliant@+1
management.wavefront.api-token=password
# Noncompliant@+1
spring.devtools.remote.secret=password

#---
spring.datasource.password=${MYSQL_PASSWORD}
spring.mail.password=${PASSWORD}
spring.sendgrid.api-key=${PASSWORD}
spring.cassandra.password=${PASSWORD}
spring.couchbase.password=${PASSWORD}
spring.data.mongodb.password=${PASSWORD}
spring.data.redis.password=${PASSWORD}
spring.data.redis.sentinel.password=${PASSWORD}
spring.datasource.hikari.password=${PASSWORD}
spring.datasource.password=${PASSWORD}
spring.datasource.tomcat.password=${PASSWORD}
spring.elasticsearch.password=${PASSWORD}
spring.h2.console.settings.web-admin-password=${PASSWORD}
spring.ldap.embedded.credential.password=${PASSWORD}
spring.ldap.password=${PASSWORD}
spring.neo4j.authentication.password=${PASSWORD}
spring.r2dbc.password=${PASSWORD}
spring.flyway.password=${PASSWORD}
spring.liquibase.password=${PASSWORD}
spring.sql.init.password=${PASSWORD}
spring.activemq.password=${PASSWORD}
spring.artemis.password=${PASSWORD}
spring.kafka.admin.ssl.key-password=${PASSWORD}
spring.kafka.admin.ssl.key-store-password=${PASSWORD}
spring.kafka.admin.ssl.trust-store-password=${PASSWORD}
spring.kafka.consumer.ssl.key-password=${PASSWORD}
spring.kafka.consumer.ssl.key-store-key=${PASSWORD}
spring.kafka.consumer.ssl.key-store-password=${PASSWORD}
spring.kafka.consumer.ssl.trust-store-password=${PASSWORD}
spring.kafka.producer.ssl.key-password=${PASSWORD}
spring.kafka.producer.ssl.key-store-key=${PASSWORD}
spring.kafka.producer.ssl.key-store-password=${PASSWORD}
spring.kafka.producer.ssl.trust-store-password=${PASSWORD}
spring.kafka.ssl.key-password=${PASSWORD}
spring.kafka.ssl.key-store-password=${PASSWORD}
spring.kafka.ssl.trust-store-password=${PASSWORD}
spring.kafka.streams.ssl.key-password=${PASSWORD}
spring.kafka.streams.ssl.key-store-key=${PASSWORD}
spring.kafka.streams.ssl.key-store-password=${PASSWORD}
spring.kafka.streams.ssl.trust-store-password=${PASSWORD}
spring.rabbitmq.password=${PASSWORD}
spring.rabbitmq.ssl.key-store-password=${PASSWORD}
spring.rabbitmq.ssl.trust-store-password=${PASSWORD}
spring.rabbitmq.stream.password=${PASSWORD}
server.ssl.key-password=${PASSWORD}
server.ssl.key-store-password=${PASSWORD}
server.ssl.trust-store-password=${PASSWORD}
spring.security.oauth2.resourceserver.opaquetoken.client-secret=${PASSWORD}
spring.security.user.password=${PASSWORD}
spring.rsocket.server.ssl.key-password=${PASSWORD}
spring.rsocket.server.ssl.key-store-password=${PASSWORD}
spring.rsocket.server.ssl.trust-store-password=${PASSWORD}
management.appoptics.metrics.export.api-token=${PASSWORD}
management.datadog.metrics.export.api-key=${PASSWORD}
management.datadog.metrics.export.application-key=${PASSWORD}
management.dynatrace.metrics.export.api-token=${PASSWORD}
management.elastic.metrics.export.api-key-credentials=${PASSWORD}
management.elastic.metrics.export.password=${PASSWORD}
management.humio.metrics.export.api-token=${PASSWORD}
management.influx.metrics.export.password=${PASSWORD}
management.influx.metrics.export.token=${PASSWORD}
management.kairos.metrics.export.password=${PASSWORD}
management.newrelic.metrics.export.api-key=${PASSWORD}
management.prometheus.metrics.export.pushgateway.password=${PASSWORD}
management.server.ssl.key-password=${PASSWORD}
management.server.ssl.key-store-password=${PASSWORD}
management.server.ssl.trust-store-password=${PASSWORD}
management.signalfx.metrics.export.access-token=${PASSWORD}
management.wavefront.api-token=${PASSWORD}
spring.devtools.remote.secret=${PASSWORD}

#---
# Noncompliant@+1
spring.datasource.password=${PASSWORD}-suffix
#---
# Noncompliant@+1
spring.datasource.password=prefix-${PASSWORD}
#---
# Compliant
spring.datasource.password=
#---
# Compliant
spring.datasource.password=${PASSWORD:defaultValue}
#---
# Noncompliant@+1
spring.datasource.password={$PASSWORD}
#---
# Compliant
spring.datasource.password=${ PASSWORD }
