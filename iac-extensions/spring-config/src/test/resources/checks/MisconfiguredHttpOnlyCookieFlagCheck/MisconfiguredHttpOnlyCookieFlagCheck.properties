# Noncompliant@+1 {{Make sure creating this cookie with "HttpOnly" flag set to false is safe.}}
server.servlet.session.cookie.http-only=false
#^[sc=0;ec=46]

#---

# Noncompliant@+1 {{Make sure creating this cookie with "HttpOnly" flag set to false is safe.}}
server.servlet.session.cookie.http-only=FALSE

#---
server.servlet.session.cookie.http-only=true

#---
server.servlet.session.cookie.http-only=

#---
http-only=false

#---
other.session.cookie.http-only=false
