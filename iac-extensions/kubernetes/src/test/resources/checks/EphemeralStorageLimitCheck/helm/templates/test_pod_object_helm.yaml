apiVersion: v1
kind: Pod
metadata:
  name: example
spec:
  containers:
    - name: {{ .Values.container.name }} # Noncompliant
#                     ^^^^^^^^^^^^^^^
      image: nginx
      volumeMounts:
        - name: ephemeral
          mountPath: "/tmp"
