apiVersion: v1
kind: Pod
metadata:
  name: example
spec:
  containers:
    - name: cpu_limit_helm
      image: nginx
      resources:
        limits: #Kubernetes verifier does not support helm files at the moment.
          ephemeral-storage: {{ .Values.container.ephemeral-storage.limit }} # Noncompliant
      volumeMounts:
        - name: ephemeral
          mountPath: "/tmp"
