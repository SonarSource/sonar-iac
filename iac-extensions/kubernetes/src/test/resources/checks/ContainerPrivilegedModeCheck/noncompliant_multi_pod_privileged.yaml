apiVersion: v1
kind: Pod
metadata:
  name: example
spec:
  containers:
    - name: foo
      securityContext:
        privileged: false
    - name: bar
      securityContext:
        privileged: true # NonCompliant
        # ^^^^^^^^^^^^^^^^
