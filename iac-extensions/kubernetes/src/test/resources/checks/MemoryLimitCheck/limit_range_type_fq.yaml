apiVersion: v1
kind: LimitRange
metadata:
  name: limit-range
spec:
  limits:
    - type: with-global-limit.example
      max:
        memory: 1Gi
      min:
        memory: 1Gi
      default:
        memory: 1Gi
      defaultRequest:
        memory: 1Gi
      maxLimitRequestRatio:
        memory: 1
