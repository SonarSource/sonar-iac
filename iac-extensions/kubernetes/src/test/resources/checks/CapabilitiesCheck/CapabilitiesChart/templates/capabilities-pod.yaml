
apiVersion: v1
kind: Pod
metadata:
  name: test-capabilities-3
spec:
  containers:
    - image: ubuntu
      command: ["sleep", "6000"]
      name: test-container
      securityContext:
      {{- if .Values.capabilities }}
        capabilities:
          add:
# Noncompliant@+2
          {{- range .Values.capabilities }}
            - {{ . | quote }}
          {{- end }}
      {{- end }}
---
